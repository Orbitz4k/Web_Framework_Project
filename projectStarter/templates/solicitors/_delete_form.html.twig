<form method="post" action="{{ path('solicitors_delete', {'id': solicitor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ solicitor.id) }}">
    <button class="btn">Delete</button>
</form>
