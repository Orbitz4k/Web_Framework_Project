<form method="post" action="{{ path('secret_mode_delete', {'id': secret_mode.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ secret_mode.id) }}">
    <button class="btn">Delete</button>
</form>
