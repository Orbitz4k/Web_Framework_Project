{% extends 'base.html.twig' %}

{% block title %}SecretMode{% endblock %}

{% block body %}
    <h1>SecretMode</h1>
<body style="background-color:red;">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ secret_mode.id }}</td>
            </tr>
            <tr>
                <th>Daredevil</th>
                <td>{{ secret_mode.Daredevil ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>SuccessfulNights</th>
                <td>{{ secret_mode.SuccessfulNights }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('secret_mode_index') }}">back to list</a>

    <a href="{{ path('secret_mode_edit', {'id': secret_mode.id}) }}">edit</a>

    {{ include('secret_mode/_delete_form.html.twig') }}
{% endblock %}
