{% extends 'base.html.twig' %}

{% block title %}Reviews{% endblock %}

{% block body %}
    <h1>Reviews</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ review.id }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ review.Rating }}</td>
            </tr>
            <tr>
                <th>Review</th>
                <td>{{ review.Review }}</td>
            </tr>
            <tr>
                <th>Rekekekemend</th>
                <td>{{ review.Rekekekemend ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reviews_index') }}">back to list</a>

    <a href="{{ path('app_reviews_edit', {'id': review.id}) }}">edit</a>

    {{ include('reviews/_delete_form.html.twig') }}
{% endblock %}
